import React from "react";
import { Box } from "@material-ui/core";

export default function Home({ classes }) {
  return (
    <Box className={classes.hero}>
      <Box>proauthenticity</Box>
    </Box>
  );
}
